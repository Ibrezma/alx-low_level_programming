#include "main.h"
/**
 * print_most_numbers - prints 0 - 9 except 2 & 4
 * Return: nothing (void)
 */

void print_most_numbers(void)
{
	for (i >= '0'; i <= '9'; i++)
	{
		if (i != '2' && i != '4')
			_putchar(i);
	}
	_putchar('\n')
}
